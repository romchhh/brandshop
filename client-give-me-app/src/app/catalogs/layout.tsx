/** Кешування оболонки сегмента (дані як і раніше з клієнтського Redux). */
export const revalidate = 120;

export default function CatalogsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
